using UnityEngine;

public class S_MoveDisc : MonoBehaviour
{
    public Transform newDiscPosition; // New position of Disc

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
